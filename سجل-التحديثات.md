# سجل التحديثات — LiftCore
**التاريخ:** 8 يونيو 2025  
**المستودع:** `github.com/mohqawashy-art/elevator-app` — فرع `main`  
**آخر commit:** `4c6365f`

---

## تطبيق التحديث على السيرفر

```bash
cd ~/liftcore/elevator-app
git pull origin main
sudo systemctl restart liftcore
```

**متطلبات إضافية (إن لم تُثبَّت بعد):**
```bash
pip install -r requirements.txt
```
(مكتبة `cryptography` مطلوبة للتوقيعات الرقمية — يوجد بديل تلقائي إن لم تتوفر)

---

## ملخص سريع (آخر جلسة عمل)

| # | الموضوع | الحالة |
|---|---------|--------|
| 1 | إصلاح الرجوع وفتح تابات جديدة | ✅ |
| 2 | إغلاق تاب الطباعة عند الرجوع | ✅ |
| 3 | فورم طلب الشراء ثنائي اللغة (عربي + إنجليزي) | ✅ |
| 4 | تقرير طلب شراء إنجليزي فقط (EN) | ✅ |
| 5 | إصلاح التقرير الإنجليزي — بدون عربي في واجهة النظام | ✅ |
| 6 | إصلاح جدول الأعطال (وميض + صعوبة فتح التقرير) | ✅ |

---

## 1. التنقل والتابات (Forms & Print)

### المشكلة
- فتح الفورمات/الطباعة في تاب جديد، والرجوع يحمّل القائمة داخل نفس التاب → تابات مكررة.
- زر إغلاق فاتورة الطباعة كان يمنع الرجوع أحياناً.

### الحل
- ملف جديد: `static/liftcore-nav.js` — رجوع ذكي (إغلاق التاب أو `history.back`).
- روابط **تعديل** المحاضر والتقارير تفتح في **نفس التاب**.
- روابط **الطباعة** تفتح عبر `window.open` مع `popup=1` ويُغلق التاب عند الرجوع.
- إصلاح زر إغلاق صفحة طباعة الفواتير.

**Commits:** `2b62a9a`, `b0e1a75`

---

## 2. طلبات الشراء — طباعة وإرسال

### أ) الفورم ثنائي اللغة (الافتراضي)
- **المسار:** `/purchase-orders/{id}/print`
- عربي وإنجليزي **معاً** في نفس المستند (مثل الفاتورة).
- اللوجو في الوسط، اسم الشركة عربي + إنجليزي، التسميات `عربي / English`.
- خطاب عربي + خطاب إنجليزي، أسماء الأصناف عربي + ترجمة إنجليزية من الكتالوج.

**Commit:** `fd427c5`

### ب) التقرير الإنجليزي فقط (جديد)
- **المسار:** `/purchase-orders/{id}/print-en`
- **من القائمة:** زر **EN** بجانب «طباعة / إرسال».
- الصفحة بالكامل LTR وإنجليزية:
  - تسميات النظام كلها إنجليزي
  - اسم الشركة والعنوان من الحقول الإنجليزية في الإعدادات
  - أسماء الأصناف إنجليزية (أو كود الصنف إن لا توجد ترجمة)
- **لا يُعرض العربي كبديل** في واجهة النظام.
- **ملاحظة:** اسم المورد والملاحظات التي تكتبها يدوياً تبقى كما أدخلتها.

**Commits:** `ae55d2c`, `4c6365f`

### إعدادات مطلوبة للتقرير الإنجليزي
من **الإعدادات → بيانات الشركة:**
- `company_name_en` — اسم الشركة بالإنجليزي
- `address_en` — العنوان بالإنجليزي

---

## 3. جدول الأعطال

### المشكلة
- الوقوف على عمود العميل + محاولة فتح التقرير → وميض وفتح نافذة العرض بالخطأ.

### الحل
- إلغاء النقر على **الصف بالكامل**.
- فتح التفاصيل من زر **العين** فقط.
- أزرار التقرير والطباعة تعمل بدون تعارض (`stopPropagation`).
- تخفيف تأثير الـ hover على الصفوف.

**Commit:** `4c6365f`  
**الملف:** `templates/faults.html`

---

## 4. محضر الصيانة والتوقيعات (جلسات سابقة — مهمة)

### قائمة الفحص الافتراضية
- كل البنود **سليم** ما عدا:
  - `5_3` (IMATIC) → **لا ينطبق**
  - `5_4` (إنذار حريق) → **لا ينطبق**
- إذا كل البنود سليم/لا ينطبق → ملاحظات الفني تُملأ تلقائياً:  
  `تم عمل الصيانة اللازمة والمصعد بحالة جيدة`

**Commits:** `2781735`, `dd32bb3`

### التوقيع الرقمي (اختياري)
- رفع توقيع مشفّر + رقم سري 6 أرقام.
- من **الإعدادات → التوقيعات** أو نموذج الفني.
- في المحضر: **إضافة توقيع** (هوية + رقم سري) أو الرسم على الكانفس.
- API: `POST /api/signatures/verify`

**Commits:** `fb3acc8` … `fad39fb`, `bff40c2`, `90c2810`

### رأس محضر الزيارة
- تم **التراجع** عن تغييرات تخطيط الرأس (اللوجو يسار فقط / تكرار الرأس بكل صفحة) بناءً على طلبك.
- الوضع الحالي: التصميم الأصلي للرأس.

**Commit:** `2c08281`

---

## 5. ملفات رئيسية تم تعديلها (آخر تحديثات)

```
static/liftcore-nav.js          ← التنقل والتابات
templates/purchase-order-print.html
templates/purchase-orders.html
templates/faults.html
templates/maintenance-visits.html
templates/visit-report.html
templates/fault-report.html
templates/invoice-print.html
app.py                          ← PO ثنائي/إنجليزي، توقيعات
```

---

## 6. أقسام مغلقة (لا تعديل إلا بطلب صريح)

- العملاء | العقود | الفنيون | المصاعد | البيانات

---

## 7. اختبار سريع بعد النشر

- [ ] فتح محضر صيانة → رجوع → لا تاب جديد مكرر
- [ ] طباعة محضر → رجوع → يُغلق تاب المعاينة
- [ ] طلب شراء → طباعة ثنائية اللغة → عربي + إنجليزي معاً
- [ ] طلب شراء → زر **EN** → كل التسميات إنجليزي
- [ ] الأعطال → زر التقرير من الجدول بدون فتح نافذة العرض
- [ ] محضر صيانة → إضافة توقيع رقمي (إن وُجدت بيانات في الإعدادات)

---

*تم إنشاء هذا الملف تلقائياً من سجل Git — للمراجعة في العمل.*
