# LiftCore — ملاحظات الإصدار
**التاريخ:** 8 يونيو 2025  
**المستودع:** https://github.com/mohqawashy-art/elevator-app  
**الفرع:** `main`

---

## أوامر النشر على السيرفر

```bash
cd ~/liftcore/elevator-app
git pull origin main
sudo systemctl restart liftcore
```

**تحقق:**
```bash
git log -1 --oneline
ls -la static/liftcore-nav.js CHANGELOG-AR.md RELEASE-NOTES.md
curl -s https://app.liftcoreapp.com/api/version
```

**إذا الموقع لم يتحدث:**
```bash
cd ~/liftcore/elevator-app && bash deploy/force_sync.sh
```

---

## ملخص التحديثات

### أ) التنقل والتابات
- إصلاح: الرجوع من الفورمات كان يفتح تاب جديد أو يكرر القائمة.
- إصلاح: تاب الطباعة يُغلق عند الضغط على «رجوع».
- ملف جديد: `static/liftcore-nav.js`
- المحاضر والتقارير تفتح في **نفس التاب**؛ الطباعة في تاب منفصل يُغلق تلقائياً.

### ب) طلبات الشراء — طباعة
| النوع | الرابط | الوصف |
|-------|--------|--------|
| ثنائي اللغة | `/purchase-orders/{id}/print` | عربي + إنجليزي معاً (افتراضي) |
| إنجليزي فقط | `/purchase-orders/{id}/print-en` | زر **EN** في قائمة الطلبات |

**للتقرير الإنجليزي:** أدخل في الإعدادات اسم الشركة والعنوان بالإنجليزي.

### ج) جدول الأعطال
- إلغاء النقر على الصف بالكامل (كان يسبب وميض ويفتح نافذة العرض).
- فتح التفاصيل من زر العين فقط.
- أزرار التقرير والطباعة تعمل بدون تعارض.

### د) محضر الصيانة (جلسات سابقة)
- افتراضيات قائمة الفحص: الكل «سليم» ما عدا IMATIC وإنذار الحريق → «لا ينطبق».
- ملاحظات الفني تُملأ تلقائياً عند اكتمال الفحص بنجاح.
- توقيع رقمي اختياري (هوية + رقم سري 6 أرقام) من الإعدادات أو نموذج الفني.
- رأس المحضر: تم الإبقاء على التصميم الأصلي (بعد طلب التراجع).

---

## Commits الأخيرة

```
9be5e37  CHANGELOG-AR.md
b0c0ebd  سجل-التحديثات.md
4c6365f  إصلاح التقرير EN + جدول الأعطال
ae55d2c  تقرير طلب شراء إنجليزي فقط
fd427c5  طلب شراء ثنائي اللغة
b0e1a75  إغلاق تاب الطباعة عند الرجوع
2b62a9a  إصلاح الرجوع والتابات
2c08281  تراجع تخطيط رأس المحضر
fb3acc8  التوقيع الرقمي
2781735  افتراضيات قائمة الفحص
```

---

## ملفات تم تعديلها

- `static/liftcore-nav.js`
- `templates/purchase-order-print.html`
- `templates/purchase-orders.html`
- `templates/faults.html`
- `templates/maintenance-visits.html`
- `templates/visit-report.html`
- `templates/fault-report.html`
- `templates/invoice-print.html`
- `app.py`

---

## اختبار سريع

1. محضر صيانة → رجوع → لا تاب مكرر
2. طباعة محضر → رجوع → يُغلق تاب المعاينة
3. طلب شراء → طباعة → عربي + إنجليزي
4. طلب شراء → زر EN → إنجليزي بالكامل
5. الأعطال → زر التقرير → يفتح مباشرة

---

## أقسام مغلقة (لا تعديل بدون طلب)

العملاء · العقود · الفنيون · المصاعد · البيانات

---

*LiftCore — mohqawashy-art/elevator-app*
